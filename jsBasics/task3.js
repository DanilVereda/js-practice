let youngAge = 16;
let isAdult = youngAge >= 18;
console.log(`If your age is ${youngAge} then return ${isAdult}`);

let equalAge = 18;
isAdult = equalAge >= 18;
console.log(`If your age is ${equalAge} then return ${isAdult}`);

let oldAge = 37;
isAdult = oldAge >= 18;
console.log(`If your age is ${oldAge} then return ${isAdult}`);
